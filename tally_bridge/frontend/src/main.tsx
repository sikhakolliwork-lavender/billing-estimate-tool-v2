import React from 'react'
import ReactDOM from 'react-dom/client'
import TallyBridge from './TallyBridge'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TallyBridge />
  </React.StrictMode>,
)